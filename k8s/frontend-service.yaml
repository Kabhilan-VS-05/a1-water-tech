apiVersion: v1
kind: Service
metadata:
  name: a1-frontend-service
  labels:
    app: a1-frontend
spec:
  type: NodePort
  selector:
    app: a1-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
